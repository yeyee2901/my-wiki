* Install package
{{
    cargo install <package>
}}

* Build package (_debug_ build)
{{
    cargo build
}}

* Build & Run (_debug_ build)
{{
    cargo run
    
    # run with arguments:
    cargo run -- <args>
}}

* Build (_release_)
{{
    cargo build --release
}}
